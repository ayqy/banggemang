{
  "name": "banggemang-benchmark",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  },
  "scripts": {
    "test": "playwright test",
    "test:json": "playwright test --reporter=json",
    "grade": "node scripts/grade-remote-branches.mjs"
  }
}

